@model IEnumerable<EliteInternationDemoWebAPP.Models.VehicleImage>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Vehicle.Model)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.VehicleImages)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Vehicle.Model)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.VehicleImages)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.VehicleImageId }) |
            @Html.ActionLink("Details", "Details", new { id=item.VehicleImageId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.VehicleImageId })
        </td>
    </tr>
}

</table>
