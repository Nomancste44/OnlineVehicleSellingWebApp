@model IEnumerable<EliteInternationDemoWebAPP.Models.SteerType>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.SteerTypeName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.SteerTypeName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.SteerTypeId }) |
            @Html.ActionLink("Details", "Details", new { id=item.SteerTypeId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.SteerTypeId })
        </td>
    </tr>
}

</table>
