@model IEnumerable<EliteInternationDemoWebAPP.Models.GearType>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.GearTypeName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.GearTypeName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.GearTypeId }) |
            @Html.ActionLink("Details", "Details", new { id=item.GearTypeId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.GearTypeId })
        </td>
    </tr>
}

</table>
